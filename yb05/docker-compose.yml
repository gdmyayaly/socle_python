# Application console : le conteneur exécute une commande puis s'arrête.
# Usage : docker compose run --rm yb05 db-info
#         docker compose run --rm yb05 db-check
services:
  yb05:
    build: .
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
