# Application console : le conteneur exécute une commande puis s'arrête.
# Usage : docker compose run --rm yb04 db-info
#         docker compose run --rm yb04 db-check
services:
  yb04:
    build: .
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
